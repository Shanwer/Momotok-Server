package common

var (
	WorkSpace string // config

	ServerInfo *serverModel = &serverModel{} // server config information

	ConfigInfo *configModel = &configModel{} // all server config information

)
